Console.WriteLine("Olá Mago, bem vindo a sua calculadora de mágia! Digite:");
    Console.WriteLine("1 - Calcular Área");
    Console.WriteLine("2 - Sair");
    string opcao = Console.ReadLine();

    switch (opcao)
    {
        case "1":
            Console.WriteLine("Digite a medida do lado do triângulo:");
            double lado = Convert.ToDouble(Console.ReadLine());

            if (lado > 0)
            {
            double alturaInterna = (lado * Math.Sqrt(3)) / 2;
                double area = (lado * alturaInterna) / 2;

                for (int i = 0; i < 3; i++) { Console.Write(". "); }

                Console.WriteLine($"\nA área calculada é: {area:F2}");
            }
            else
            {
                Console.WriteLine("Erro: O lado deve ser maior que zero.");
            }
            break;

        case "2":
            break;

        default:
            Console.WriteLine("Opção inválida!");
            break;
    }

